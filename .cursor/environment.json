{
  "install": "set -e; if ! command -v python3 >/dev/null 2>&1; then sudo apt-get update && sudo apt-get install -y python3 python3-pip python3-venv; fi; python3 -m pip install --upgrade pip; python3 -m pip install -r lost_and_found_web/requirements.txt",
  "start": "python3 --version && python3 -m pip --version"
}
